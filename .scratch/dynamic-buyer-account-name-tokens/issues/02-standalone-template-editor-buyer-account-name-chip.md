# 02 — Standalone Template Editor "Buyer Account Name" Token Chip

**What to build:** In the standalone `TipTapTemplateEditor`, provide the `"Buyer Account Name"` (`buyer_name`) token chip in the token insertion menu and render it as an uneditable interactive pill element in the editor canvas, keeping it as an unbound dynamic placeholder during standalone template authoring.

**Blocked by:** None — can start immediately.

**Status:** completed

- [x] `"Buyer Account Name"` is listed in the dynamic token palette within `TipTapTemplateEditor`.
- [x] Inserting `"Buyer Account Name"` inserts an interactive pill element into the WYSIWYG editor canvas representing `{{buyer_name}}`.
- [x] Creating or editing email templates in standalone mode does not mandate selecting buyer data.
