# 09 — Modals & Misc Components Light Theme

**What to build:** All remaining components with hardcoded dark classes render correctly in light mode: `QuickBidModal`, `BuyerBidModal`, `SmartAudienceLotSelector`, `BuyerListManagerModal`, `LiveDevicePreview`, and `InteractiveTour`. Each modal, selector panel, and overlay is polished in both themes.

**Blocked by:** 01 — Semantic Token Layer Refactor

**Status:** completed

- [x] `QuickBidModal` and `BuyerBidModal` render with a light card background, readable input fields, and correct button colours in light mode
- [x] `SmartAudienceLotSelector` panel renders with a light surface and correct list/badge contrast
- [x] `BuyerListManagerModal` renders with a light card and legible form controls
- [x] `LiveDevicePreview` device frame and inner preview render correctly against a light background
- [x] `InteractiveTour` overlay, tooltip cards, and highlight rings are readable in light mode
- [x] Dark mode unchanged for each component — no regression
